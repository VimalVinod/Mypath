{
  "name": "mypath",
  "version": "1.0.0",
  "private": true,
  "description": "MyPath - Smart Government Exam Discovery & Tracking Platform",
  "scripts": {
    "dev": "npm --prefix codes run dev",
    "build": "npm --prefix codes run build",
    "preview": "npm --prefix codes run preview"
  }
}
